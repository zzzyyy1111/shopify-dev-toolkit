interface:
  display_name: "Shopify Schema 检查助手"
  short_description: "检查 Shopify Theme Schema、模板 JSON 与设置值是否合法"
  default_prompt: "Use $shopify-schema-validator to validate this Shopify theme before preview."
policy:
  allow_implicit_invocation: true
